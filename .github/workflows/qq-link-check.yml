name: qq-link-check
on:
  workflow_call:
    inputs:
      args:
        type: string
        default: "--verbose --no-progress --max-concurrency 8 ./**/*.md"

jobs:
  link-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Lychee link check
        uses: lycheeverse/lychee-action@v2
        with:
          args: ${{ inputs.args }}
          fail: true
